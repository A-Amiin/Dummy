import { createSlice } from '@reduxjs/toolkit';

const BlogSlice = createSlice({
    name: 'Blog',
    initialState: {},
    reducers: {},
    extraReducers: () => { },
});

export default BlogSlice.reducer;